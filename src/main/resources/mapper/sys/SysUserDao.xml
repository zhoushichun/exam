<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.garm.modules.sys.dao.SysUserDao">
	<!-- 查询用户的所有权限 -->
	<select id="queryAllPerms" resultType="string">
		select m.perms from sys_user_role ur
			LEFT JOIN sys_role_menu rm on ur.role_id = rm.role_id
			LEFT JOIN sys_menu m on rm.menu_id = m.menu_id
		where ur.user_id = #{userId}
	</select>

	<!-- 查询用户的所有菜单ID -->
	<select id="queryAllMenuId" resultType="long">
		select distinct rm.menu_id from sys_user_role ur
			LEFT JOIN sys_role_menu rm on ur.role_id = rm.role_id
		where ur.user_id = #{userId}
	</select>

	<select id="queryByUserName" resultType="com.garm.modules.sys.entity.SysUserEntity">
		select * from sys_user where username = #{username}
	</select>


	<select id="queryList" resultType="com.garm.modules.sys.entity.SysUserEntity">
		select a.*, c.role_name roleName from sys_user a
		left join sys_user_role b on a.user_id = b.user_id
		left join sys_role c on c.role_id = b.role_id
		<where>
			<if test="nickname!=null and nickname!=''">
				and a.nickname LIKE CONCAT('%',#{nickname},'%')
			</if>
			<if test="mobile!=null and mobile!=''">
				and a.mobile  LIKE CONCAT('%',#{mobile},'%')
			</if>
			<if test="createUserId!=null and createUserId!=''">
				and a.create_user_id = #{createUserId}
			</if>
			<if test="roleId!=null and roleId!=''">
				and b.role_id = #{roleId}

			</if>
		</where>
	</select>

</mapper>
